#!/usr/bin/env bash
# This script configures the /etc/hosts file so that:
# - localhost resolves to 127.0.0.2
# - facebook.com resolves to 8.8.8.8

# Remove existing entries for localhost and facebook.com to prevent duplicates
sudo sed -i '/\blocalhost\b/d' /etc/hosts
sudo sed -i '/\bfacebook.com\b/d' /etc/hosts

# Append the new configurations to the /etc/hosts file
echo "127.0.0.2 localhost" | sudo tee -a /etc/hosts > /dev/null
echo "8.8.8.8 facebook.com" | sudo tee -a /etc/hosts > /dev/null
